import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'With Love, Irene',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
